%!name:inverse_elements
%!need:element_functions

\stitle{Inverse Elements}

\ssection{Why}

Is the inverse
of an element function
the element function
of a different element?

\ssection{Definition}

TODO

\ssubsection{Notation}

Let $(A, +)$ be an algebra.
Let $a \in A$.
If the inverse element
for $a$ exists and is unique we denote it by $\inv{a}$.
