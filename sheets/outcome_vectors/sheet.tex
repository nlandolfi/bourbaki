%!name:outcome_vectors
%!need:outcome_variables
%!need:vectors
%!need:event_probabilities

\ssection{Why}

\ssection{Definition}

An \ct{outcome vector}{} is kind
of outcome variable where the set
of values is the $n$-dimensional
tuples of real numbers, for
$n = 1, 2, \dots$.

\ssection{Notation}

Let $R$ denote the set of
real numbers and let $\PE$
denote the event probability
function

Let


