%!name:joint_distributions
%!need:probability_distributions
%!need:direct_products

\ssection{Why}

\ssection{Definition}

Consider a distribution over a product
of $n$ sets, where the product is indexed
by the first $n$ natural numbers.
We call the distribution a
\ct{joint distribution}{} with
$n$ components.
