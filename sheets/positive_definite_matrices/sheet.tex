%!name:positive_definite_matrices
%!need:quadratic_forms
%!need:symmetric_real_matrices
%!refs:friedrich_pukelsheim∕optimal_design_of_experiments∕01_experimental_designs_in_linear_models

% future editions should flip this!! 

% PSD should be first! 

\section*{Definition}

Let $A \in \mathbfsf{S} ^n$ (i.e., $A \in \R ^{n \times  n}$ and symmetric).
$A$ is \t{positive definite} if, for all $x \neq 0 \in \R ^{d}$,
  \[
x^\top  A x > 0.
  \]
$A$ is \t{positive semidefinite} (or \t{nonnegative definite}) if, for all $x \in \R ^{d}$
  \[
x^\top  A x \geq 0.
  \]

\subsection*{Notation}

We denote the set of positive definite $d$ by $d$ matrices by $\mathbfsf{S} _{++}^d$.
We denote the set of positive semidefinite $d$ by $d$ matrices by $\mathbfsf{S} _{+}^d$.

\blankpage