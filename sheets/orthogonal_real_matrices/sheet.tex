%!name:orthogonal_real_matrices
%!need:orthonormal_set_of_real_vectors

\section*{Definition}

An \t{orthogonal matrix} is a matrix $U \in \R ^{n \times n}$ whose columns are orthonormal.
In other words, an orthogonal matrix has $n$ columns, and so its columns are a basis for $\R ^n$.

\blankpage