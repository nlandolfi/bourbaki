%!name:graph_complements
%!need:set_complements
%!need:set_unions
%!need:graphs

\ssection{Why}

TODO.
For split graphs.

\ssection{Definition}

The \t{complement} of an undirected graph $(V, E)$ the graph $(V, F)$ where $F$ is the complement of $E$ in the set $\Set*{\set{u, v}}{u, v \in V, u \neq v}$.
