
\section*{Result}

For a given subspace $S$, the number of vectors in any basis is the same.
We call this number the \t{dimension} of the subspace of $S$.

\subsection*{Notation}

We denote the dimension of $S$ by $\dim S$

\blankpage
%macros.tex
%\renewcommand{\dim}{\mathword{dim}}
