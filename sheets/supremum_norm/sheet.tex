
\section*{Why}

We want a norm on the vector space of continuous functions.

\section*{Definition}

Consider a function from a closed real interval to the real numbers.
The \t{absolute supremum} of the function is the supremum of the absolute value of its results on the interval.
Since the function is continuous and defined on a closed interval, the supremum is finite.

\begin{proposition}
The functional mapping $f \in C[a, b]$ to its absolute supremum is a norm.
\end{proposition}

\begin{proof}Let $R$ denote the set of real numbers.
Define $\phi : C[a, b] \to R$ by:
\[
\phi (f) = \sup \Set{\abs{f(x)}}{x \in [a, b]}.
\]
\begin{enumerate}
  \item $\abs{f(x)} \geq 0$ for all $x \in [a, b]$, so $\phi (f) \geq 0$.
  \item If $\phi (f) = 0$ then $\abs{f(x)} \leq 0$ for all $x$ and so $f(x) = 0$ for all $x \in [a, b]$.
If $f = 0$, then $\abs{f(x)} = 0$ for all $x \in [a, b]$
  \item For all $\alpha $ real, $\abs{\alpha  f(x)} = \abs{\alpha }\abs{f(x)}$.
So $\phi (\alpha  f) = \abs{\alpha }\phi (f)$
  \item For all $f, g \in C[a, b]$, and $x \in [a, b]$, $\abs{f(x) + g(x)} \leq \abs{f(x)} + \abs{g(x)}$ by the triangle inequality for absolute value.
Thus,
\[
\begin{aligned}
\phi (f + g)
&\leq \sup \Set{\abs{f(x)} + \abs{g(x)}}{x \in [a, b]} \\
&\leq \sup \Set{\abs{f(x)}}{x \in [a,b]} + \sup \Set{\abs{g(x)}}{x \in [a, b]} \\
&= \phi (f) + \phi (g)
\end{aligned}
\]
\end{enumerate}\end{proof}
We call the functional $\phi $ defined above the \t{supremum norm}.

\subsection*{Notation}

Let $f \in C[a, b]$.
We denote the supremum norm of $f$ by $\norm{f}_{\sup}$.

\blankpage