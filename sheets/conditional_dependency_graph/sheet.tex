%!name:conditional_dependency_graph
%!need:graphs


\ssection{Definition}

Consider a set of two
or more
random variables.
Call two random variables
in this set \ct{dependent}{} if
they are not condiitionally
independent, given all the other
variables.
Call the undirected graph
induced by this relation
the
\ct{conditional dependency graph}{}
of the random variables.
